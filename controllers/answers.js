const { answers } = require('../models');

module.exports = {
  getAnswers: (params, queryParams, req, res) => {
    // do stuff
  },
  addAnswer: (params, bodyParams, req, res) => {
    // do stuff
  },
  markHelpful: (params, req, res) => {
    // do stuff
  },
  report: (params, req, res) => {},
};
